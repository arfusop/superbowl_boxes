import React from 'react';
/** @jsx jsx */
import {jsx, css} from '@emotion/core'

const Header = () => {
  return (
    <div
      css={css ` display: flex; flex-direction: row; justify-content: center; align-items: centerl `}>
      SUPER BOWL BOXES
    </div>
  )
}

export default Header;